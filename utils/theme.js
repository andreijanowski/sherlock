const breakpoints = ["40em", "52em", "64em"];

const radius = [];

const fontSizes = [];

const colors = {};

// eslint-disable-next-line import/prefer-default-export
export const theme = {
  breakpoints,
  colors,
  radius,
  fontSizes
};
